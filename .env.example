# API Configuration - Using Netlify proxy (production) or direct URL (development)
# Production: /api (proxied by Netlify to http://75.119.151.238:5001)
# Development: http://75.119.151.238:5001 (direct access)
VITE_API_BASE_URL=/api

# Token Storage Strategy
# Options: localStorage | sessionStorage | memory
VITE_TOKEN_STORAGE=localStorage

# Token Refresh Configuration
VITE_TOKEN_REFRESH_ENDPOINT=/refresh

# Optional: Token expiry buffer (seconds before expiry to refresh)
VITE_TOKEN_REFRESH_BUFFER=30